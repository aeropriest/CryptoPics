import { View, Text } from "react-native";
import React from "react";

export default function BidDetails({ bids }) {
  console.log(bids);
  return (
    <View>
      <Text>BidDetails</Text>
    </View>
  );
}
